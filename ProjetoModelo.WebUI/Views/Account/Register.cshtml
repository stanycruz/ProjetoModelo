@model ProjetoModelo.WebUI.Models.RegisterViewModel
@{
    ViewData["Title"] = "Registrar nova conta";
}

<section class="bloco formularios">

    <div class="row container">

        <div class="col s12 center">
            <h2 class="titulo light white-text">@ViewData["Title"]</h2>
        </div>

        <div class="col l3"></div>

        <div class="col s12 m12 l6 card-panel">

            @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary()

                <div class="input-field col s12">
                    @Html.EditorFor(m => m.Email, new { htmlAttributes = new { @autocomplete = "off", @type = "email" } })
                    @Html.LabelFor(m => m.Email)
                </div>

                <div class="input-field col s12">
                    @Html.PasswordFor(m => m.Password)
                    @Html.LabelFor(m => m.Password)
                </div>

                <div class="input-field col s12">
                    @Html.PasswordFor(m => m.ConfirmPassword)
                    @Html.LabelFor(m => m.ConfirmPassword)
                </div>

                <p class="col s12">
                    <button class="btn blue-logo right" type="submit">
                        Registrar
                        <i class="material-icons right">send</i>
                    </button>
                </p>
            }

        </div>
    </div>
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
